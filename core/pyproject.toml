[build-system]
requires = ["maturin>=1.7,<2.0"]
build-backend = "maturin"

[project]
name = "brandobot_core"
version = "0.1.0"
description = "Native chess engine core for brandobot."
requires-python = ">=3.11"

[tool.maturin]
features = ["pyo3/extension-module"]
