<!-- foundry-seed: log v1 -->

# Knowledge log

Changes to the knowledge base, newest first. A reserved OKF file — no frontmatter.
Add a `## YYYY-MM-DD` heading (newest first) with `- **Creation**` / `- **Update**` entries.

## 2026-06-14

- **Creation** — bootstrapped the knowledge base: `glossary` (chess + search
  vocabulary, neutral-engine polarity), `validation`, `coe-template`, and the
  `README` index.
